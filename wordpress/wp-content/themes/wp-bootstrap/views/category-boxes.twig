{% block content %}
    <section class="boxes">
      <div class="container">
        <div class="row">

        {% for category in categories %}
            <div class="col-md-4">
                <div class="box">
                    <i class="fa fa-users" aria-hidden="true"></i>
                    <h3>
                        {{category.cat_name}}
                    </h3>
                    <p>
                        {{category.cat_name}}
                    </p>
                </div>
                </div>
        {% endfor %}
        </div>
      </div>
    </section>

{% endblock %}